## Connect to existing sprite
```
sprite console -s test
```

Once connected, configure MCP servers in `~/work/.gemini/settings.json`.

Then start gemini-cli agent and call tools. Use this prompt:
```
Get sensor data and then analyze the bearing health using that data.
```

## Make sure the MCP servers are running and connected
```
/mcp list
```

